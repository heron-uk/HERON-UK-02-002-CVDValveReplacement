# Study diagnostics shiny

1. Open in Rstudio the PhenotypeRShiny.Rproj project
2. Copy you csv of diagnostics into the data/raw directory
3. Rebuild the R-environment from the provided lock-file in renv.lock with `renv::restore()`.
4. Open the "global.R" script and run the app (top-left button in the R the script).
